// string file_name = Util::getExecutableDirectory() +
//                    "/../../problemsData/dados_exemplo.csv";
// std::ifstream target_file(file_name);

// if (!target_file.is_open()) {
//     std::cerr << "Erro ao abrir o arquivo: " << file_name << std::endl;
//     return 1;
// }

// vector<string> cols_names;
// unordered_map<string, vector<string>> cols_data =
//     Util::loadCSVData(target_file, cols_names);

// target_file.close();

// Util::printColsData(cols_data, cols_names);
